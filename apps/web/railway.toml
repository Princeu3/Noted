[build]
builder = "nixpacks"
buildCommand = "cd ../.. && bun install && bun run --filter @noted/web build"

[deploy]
startCommand = "npx serve dist -s -l 3000"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
